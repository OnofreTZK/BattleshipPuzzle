#include "ships.h"


Board create_Board( Board board, size_t row, size_t col )
{
    
    
    for( int vec = 0; vec < board.armada.size(); vec++ )
    {
        for( int i = 0; i < row; i++ )
        {
            
        }
    } 
    return board;
}


